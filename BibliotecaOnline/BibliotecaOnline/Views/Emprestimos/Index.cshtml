@model IEnumerable<BibliotecaOnline.Models.Emprestimo>
@{
    ViewData["Title"] = "Lista de Empréstimosw";
}

<div class="container mt-5">
    <h2 class="mb-4 text-center">Lista de Empréstimos</h2>

    <div class="text-end mb-3">
        <a asp-action="Create" class="btn btn-success">Adicionar Novo Empréstimo</a>
    </div>

    <table class="table table-bordered table-striped table-hover">
        <thead class="table-dark text-center">
            <tr>
                <th>Código</th>
                <th>Data de Retirada</th>
                <th>Data de Entrega</th>
                <th>Ações</th>
            </tr>
        </thead>
        <tbody class="text-center">
            @foreach (var item in Model)
            {
                <tr>
                    <td>@item.Codigo</td>
                    <td>@item.DataRetirada.ToString("dd/MM/yyyy")</td>
                    <td>@item.DataEntrega.ToString("dd/MM/yyyy")</td>
                    <td>
                        <a asp-action="Edit" asp-route-id="@item.Codigo" class="btn btn-primary btn-sm">Editar</a>
                        <a asp-action="Details" asp-route-id="@item.Codigo" class="btn btn-info btn-sm">Detalhes</a>
                        <a asp-action="Delete" asp-route-id="@item.Codigo" class="btn btn-danger btn-sm">Excluir</a>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>